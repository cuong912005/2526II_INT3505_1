# OpenAPI 3.0 - Book Management API

## Installation

```bash
# Cài Swagger UI (Docker)
docker run -p 8080:8080 -e SWAGGER_JSON=/data/openapi.yaml \
  -v $(pwd):/data swaggerapi/swagger-ui

# Truy cập http://localhost:8080
```
